import 'package:flutter/material.dart';

/// ALU Brand Colors and App Theme Colors
class AppColors {
  // Primary ALU Brand Colors
  static const Color primary = Color(0xFF003366);      // Dark Blue
  static const Color secondary = Color(0xFFE31837);    // Red
  static const Color accent = Color(0xFF00C853);       // Green
  static const Color background = Color(0xFFF5F7FA);   // Light Gray
  
  // Status Colors
  static const Color success = Color(0xFF00C853);      // Green
  static const Color warning = Color(0xFFFFA726);      // Orange
  static const Color danger = Color(0xFFE31837);       // Red
  static const Color info = Color(0xFF2196F3);         // Blue
  
  // Priority Colors
  static const Color highPriority = Color(0xFFE31837);    // Red
  static const Color mediumPriority = Color(0xFFFFA726);  // Orange
  static const Color lowPriority = Color(0xFF757575);     // Gray
  
  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textDisabled = Color(0xFFBDBDBD);
  
  // UI Element Colors
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color divider = Color(0xFFE0E0E0);
  static const Color border = Color(0xFFE0E0E0);           // ← ADD THIS
  static const Color inputFill = Color(0xFFF5F5F5);        // ← ADD THIS
  
  // Session Type Colors
  static const Color classSession = Color(0xFF2196F3);
  static const Color masterySession = Color(0xFF9C27B0);
  static const Color studyGroup = Color(0xFF4CAF50);
  static const Color pslMeeting = Color(0xFFFF9800);
}