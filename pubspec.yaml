name: alu_acabuddy
description: A personal academic assistant app for ALU students to manage assignments, schedules, and attendance.


version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.1
  
  # Date & Time Utilities
  intl: ^0.18.1
  
  # UUID for generating unique IDs
  uuid: ^4.2.1
  
  # Icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # Placeholder for assets
  # assets:
  #   - assets/images/
  #   - assets/icons/